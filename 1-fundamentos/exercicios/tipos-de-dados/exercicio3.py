# Crie um dicionário chamado `pessoa` com as chaves `nome`, `idade` e `altura`. Atribua valores e imprima o conteúdo.

pessoa = {
    "nome": "Emily",
    "idade": 27,
    "altura": 1.67}

print(f'''\n📚 Dicionário de Pessoa:
      Nome: {pessoa["nome"]}
      Idade: {pessoa["idade"]}
      Altura: {pessoa["altura"]} m.
      ''')